/**
 * Stamped by the pre-paint boot script in intro-panel.tsx so the provider can
 * measure the CSS build sequence from when the panel actually started
 * animating, rather than from hydration.
 */
interface Window {
  __acobIntroStart?: number;
}
